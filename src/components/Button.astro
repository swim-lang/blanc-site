---
interface Props {
  href?: string;
  variant?: "solid" | "solid-paper" | "outline-ink" | "outline-paper";
  class?: string;
}
const { href = "#", variant = "outline-ink", class: cls, ...rest } = Astro.props;
---

<a href={href} class:list={["btn", `btn--${variant}`, cls]} {...rest}>
  <slot />
</a>
